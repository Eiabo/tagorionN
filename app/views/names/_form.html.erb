<%= form_for @name do |f| %>
	<% if @name.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@name.errors.count, "error") %> prohibited this article from being saved: </h2>
		<ul>
		<% @name.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
	<% end %>

	<p>
		<%= f.label :namalengkap %><br>
		<%= f.text_field :namalengkap %>
	</p>

	<p>
		<%= f.label :npm %><br>
		<%= f.text_area :npm %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>